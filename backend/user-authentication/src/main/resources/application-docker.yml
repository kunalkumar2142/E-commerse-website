spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

application:
  security:
    jwt:
      expiration: 86400000

eureka:
  client:
    service-url:
      defaultZone: http://shopre-service-discovery:8761/eureka/
